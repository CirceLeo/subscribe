function UserEditBox({closeModal}) {
    
    return (
        <div className="user-edit-sub">
            <button className="close-button" onClick={closeModal}>X</button>
            <p>hi mom!</p>
        </div>
    )
}
export default UserEditBox